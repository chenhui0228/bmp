diff a/client/main.py b/client/main.py	(rejected hunks)
@@ -67,6 +67,9 @@
     if 'start' == sys.argv[1]:
         daemon = Daemon(pid_file,  mylogger, version)
         daemon.start()
+    elif  'restart' == sys.argv[1]:
+        daemon = Daemon(pid_file, mylogger, version)
+        daemon.restart()
     elif 'stop' == sys.argv[1]:
         daemon = Daemon(pid_file,  mylogger, version)
         time.sleep(0.2)
